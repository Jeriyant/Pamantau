# Local / runtime
database/pamantau.json
database/auth_attempts.json
database/*.lock
database/background.lock
database/topology-canvas.*
database/headless-snapshot-*
database/runtime-base-url.json
.update-progress.json

# Editor / OS
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
*.swp
*~

# Optional local topology exports (keep out of source releases)
CUSJ.json
JNET.json

# Commit via Desktop/token with workflow scope:
.github/workflows/

# Build / release artifacts
dist-pack/
pamantau-dist.zip
# Legacy release archives may still exist locally after removing portable support.
Pamantau-Portable-*.zip
